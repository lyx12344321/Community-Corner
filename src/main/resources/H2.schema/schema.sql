-- 如果需要手动创建架构 (但你已经创建过了)

-- create table USERS
-- (
--     ID           INTEGER auto_increment,
--     ACCOUNT_ID   CHARACTER VARYING(100),
--     NAME         CHARACTER VARYING(100) not null,
--     TOKEN        CHARACTER(36),
--     GMY_CREATE   BIGINT,
--     GMT_MODIFIED BIGINT,
--     constraint ID_PK
--         primary key (ID)
-- );

